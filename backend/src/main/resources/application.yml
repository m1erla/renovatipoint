spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/werkspot
    username: postgres
    password: mierla
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

application:
  security:
    jwt:
      secret-key: yxFh9a2cayJdQhmqeE6r4Es1StFwwZFHMLNHSsiT41E=
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days